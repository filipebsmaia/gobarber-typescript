name: CI
on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      
    - name: Setup Node.js environment
      uses: actions/setup-node@v2.1.0
      with:
        node-version: 12.x
        
    # Instalar as dependêncis
    - name: Install dependecies
      run: yarn
      
    # Executar a build
    - name: Run build
      run: yarn build
      
    # Copiar a pasta dist pra dentro do Servidor- name: copy file via ssh password
    - name: Copy dist to Digital Ocean
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        port: ${{ secrets.SSH_PORT }}
        key: ${{ secrets.SSH_KEY }}
        source: "dist"
        target: "test"
      
    # Subistituir o código existente na pasta "dist" pelo novo código
    # Executar "yarn" no servidor
    # Executar as migrations
    # Reiniciar o servidor node
